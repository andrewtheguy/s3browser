# S3 Browser Environment Variables
# Copy this file to .env and fill in the values

# Required: Encryption key for S3 credentials (must be at least 32 bytes)
# If hex-encoded, this means 64 hex characters minimum
# Generate a secure key with: openssl rand -hex 32
S3BROWSER_ENCRYPTION_KEY=REPLACE_ME_WITH_ACTUAL_KEY
